#ifdef GL_FRAGMENT_PRECISION_HIGH
  precision highp float;
#else
  precision mediump float;
#endif

uniform sampler2D u_texture;
uniform vec4 u_scaling;

// Generated by https://github.com/tuzz/gaussian_blur_generator

void main() {
  vec2 coords = gl_FragCoord.xy * u_scaling.xy;

  float one_pixel = u_scaling.y;
  float offset1 = one_pixel * 1.2;

  gl_FragColor = (
    0.3125 * texture2D(u_texture, vec2(coords.x, coords.y - offset1)) +
    0.375 * texture2D(u_texture, coords) +
    0.3125 * texture2D(u_texture, vec2(coords.x, coords.y + offset1))
  );
}
